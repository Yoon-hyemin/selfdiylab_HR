# flex 채용(Recruiting) 구조 상세 가이드

실제 flex.team 로그인 계정에서 채용 공고를 직접 만들고, 후보자를 등록하고, 전형 단계를 이동시켜보며 확인한 내용입니다.

---

## 0. 전체 구조 개요

좌측 사이드바 "채용" 클릭 시 상단에 3개 탭:

**후보자 / 공고 / 사이트**

세 탭은 종속 관계입니다.

```
공고(직무·직군·전형단계·지원정보 설정) ──▶ 후보자 등록 시 공고를 선택 ──▶ 후보자가 그 공고의 전형단계 파이프라인 위에서 이동
                                                              └▶ 사이트(공개 채용페이지)에 그 공고가 노출
```

즉, **공고가 없으면 후보자를 등록할 수 없습니다.** "후보자 등록하기"를 눌렀을 때 공고가 하나도 없으면 바로 이런 모달이 뜹니다.

> **아직 공고가 하나도 없어요**
> 후보자를 등록하려면 먼저 공고 탭으로 이동해서 공고를 만들어보세요.
> [공고 탭으로 이동하기] / 취소

---

## 1. 후보자(Candidates) 탭

### 1-1. 목록 구조
- 상단 서브탭: **전형 진행 중 / 전형 종료**
- 목록은 **전형단계별로 그룹핑**되어 표시됩니다. 예: "접수 1", "서류 전형 1" 처럼 단계명 + 인원수 헤더 아래에 후보자 카드가 나열됩니다.
- 후보자 카드에 표시되는 정보: 이름, 지원한 공고명(뱃지), 받은 메일 수(✉ 아이콘), 등록일(예: "8월 3일"), 담당자 아바타
- 우측 패널 "공고" — **내 공고 / 전체 공고** 토글 + 검색창 + 공고별 후보자 수. 특정 공고를 클릭하면 그 공고의 후보자만 필터링됩니다.

### 1-2. 필터
기본 필터바: **담당자**(칩, x로 제거 가능) · **전형단계** · **직무** · **+ 필터 추가**

- **전형단계** 드롭다운(체크박스形): 접수 ○ / 처우협의 ● / 최종합격 ✓ / 최종탈락 ⊘ — 4개 고정 단계 + 공고별로 추가한 커스텀 단계가 함께 나열됩니다.
- **직무** 드롭다운: 검색창 "직무 검색" + "총 N개 선택". 회사에 등록된 직무 태그 중에서 검색·다중선택.
- **+ 필터 추가** 클릭 시 추가 가능한 필터 목록: **직군 / 병역사항 / 계약 형태 / 지원 경로 / 장애·보훈여부 / 중복 지원 여부 / 삭제 예정**

### 1-3. 후보자 등록하기 (공고와의 연결 지점)
"+ 후보자 등록하기" 버튼 → 모달:

| 필드 | 설명 |
|---|---|
| 이름* | 필수 |
| 채용 공고* | 필수. 검색형 드롭다운으로 기존 공고 중 선택 |
| 이메일 | 선택 |
| 연락처 | 선택 |
| 지원 경로 | 드롭다운(검색 및 추가 가능). 기본 프리셋: **직접지원 / 리쿠르터 / 사내추천 / 헤드헌터 / 채용 포탈 / 기타** (각 항목 "···"로 이름 수정 가능, 검색창에서 자유 텍스트로 새 경로 추가도 가능) |
| 등록일* | 필수, 기본값 오늘 날짜 |
| 제출 자료 | **채용 공고를 선택하면 그 공고의 "지원 제출자료" 설정이 그대로 나타납니다.** 예: 공고에서 "이력서"를 필수로 설정했다면 "이력서*" 항목이 나타나고, 각 항목마다 **파일 / 링크** 두 방식 중 선택해 첨부 가능 (파일 첨부는 100MB 미만) |
| 이어서 등록하기 | 토글. "여러 후보자를 연달아 등록할 수 있어요." — 켜두면 저장 후 모달이 닫히지 않고 다음 후보자를 바로 등록 |

저장 시 토스트: "후보자를 등록했어요." 후보자는 해당 공고의 파이프라인 맨 첫 단계(접수)에 자동으로 들어갑니다.

### 1-4. 후보자 상세 패널
후보자 이름 클릭 → 우측에서 슬라이드인:

- 헤더: 이름 / 지원 공고명(부제) / 전체화면 아이콘 / "···" 더보기 / 닫기
- **전형단계 컨트롤**: 현재 단계 드롭다운(예: "접수") + **"다음으로 ≫"** 버튼 — 클릭 한 번으로 다음 단계로 이동. 이동 시 토스트("후보자를 OO 전형으로 변경했어요.")와 타임라인 로그가 동시에 남음
- 탭: **후보자 정보 / 평가 / 메일 / 타임라인**

**후보자 정보 탭**
- 지원 정보: 이메일 / 연락처 / 등록일(등록 당일이면 "오늘 지원" 뱃지) / 지원 경로 / 담당자
- **상세 정보**(접기/펼치기): 경력 정보(기간/정보없음), 학력(기간/정보없음), 기타 — 병역/장애/보훈 각각 "선택하기" 링크로 값 지정
- **사전 질문**(접기/펼치기): 공고에 설정한 사전질문의 답변이 표시됨. 답변이 없으면 "등록된 사전 질문이 없어요."
- **첨부 파일·링크**: 등록 시 제출한 자료 목록 (예: "이력서" + 링크/파일, 새 창 열기 아이콘 + "···" 메뉴)
- 최하단 **내 메모**(비공개 메모, 접기/펼치기)

**평가 탭**
- "평가기록 0/2" 처럼 공고에서 설정한 평가형 전형단계 수만큼 카드가 나열됨
- 서류형 단계: "평가자가 없어요." + "평가자 지정하기" 버튼
- 인터뷰형 단계: "인터뷰 일정이 없어요." + "인터뷰 일정 만들기" 버튼
- 즉, **공고의 전형단계 설정(평가 템플릿/평가자/인터뷰 여부)이 후보자별 평가 체크리스트로 그대로 이어짐**

**메일 탭**
- "보낸 메일 0" + "메일 쓰기" 버튼. 후보자에게 직접 이메일 발송 가능 (플랫폼 내 발송)

**타임라인 탭**
- 활동 로그(예: "윤혜민님이 후보자를 추가했어요.", "윤혜민님이 서류 전형 전형으로 변경했어요." + 상대시간)
- 하단 "전달사항 입력..." + 전송 아이콘 — 팀 내부 코멘트 스레드

---

## 2. 공고(Job Postings) 탭

### 2-1. 목록
- 상단 필터: **전체 / 공개 / 비공개** + 담당자 · 직무 · 직군 · 계약 형태 필터 + 초기화
- 빈 상태: "채용 공고가 없어요." + "공고 만들기"
- 만들어진 공고는 **비공개 / 공개** 그룹으로 접기/펼치기 되어 나열되고, 카드에 다음이 표시됨:
  - 제목 + 공개상태 뱃지
  - 부제: `직군 · 근무기간 · 근무형태 · 담당자 - 이름`
  - **전형단계별 후보자 수 퍼널**: 접수 0 › 서류 전형 0 › 1차 면접 0 › 처우협의 0 › 최종합격 0 › 최종탈락 0 (설정한 커스텀 단계까지 전부 순서대로 표시)
  - 우측: 전형 종료 인원(최종합격·최종탈락) / 전형 진행 중 인원
  - "수정" 버튼

### 2-2. 공고 만들기 — 우측 슬라이드 패널, 3단계 탭

패널 상단 공통 요소 (모든 탭에서 항상 보임):
- **공개여부** 드롭다운(비공개 기본값)
- 제목 입력란 ("공고 제목을 입력해 주세요.")
- 정보 칩 6개: **담당**(기본값 나 자신) · **형태** · **기간** · **직무** · **직군**

각 칩을 클릭하면 인라인으로 편집됩니다.

| 칩 | 옵션/동작 |
|---|---|
| 담당 | 담당자 지정 (조직 구성원 중 선택) |
| 형태 | 드롭다운: **정규직 / 계약직 / 프리랜서 / 파트타임 / 인턴십 / 파견 근로자** |
| 기간 | 드롭다운: **상시채용**(기본값) / **기간 선택**(선택 시 시작일·종료일 캘린더 표시) |
| 직무 | 검색 또는 추가하는 자유 태그. 새 직무를 추가하면 확인 모달 **"입력하신 직무를 추가할까요? 추가하면 플렉스 인사 정보에 반영돼요."**가 뜸 → 즉, **직무 태그는 회사 전체 인사(HR) 데이터와 공유되는 마스터 데이터**이며, 후보자 필터의 "직무"도 같은 태그를 사용 |
| 직군 | 검색 또는 추가하는 자유 태그. 안내 문구: "후보자가 직군별로 공고를 확인할 수 있어요." → **채용 사이트(공개 페이지)에서 후보자가 직군 단위로 공고를 필터링해 볼 수 있는 상위 분류**. 직무와 달리 추가 시 확인 모달 없이 즉시 추가됨 (인사 정보에는 반영 안 됨) |

#### 탭 1. 공고 본문
- 리치 텍스트 에디터 (텍스트 스타일 / B·i·U / 이미지 / 링크 / 목록·번호목록·인용 등)
- 플레이스홀더: "공고 본문을 작성해 주세요."

#### 탭 2. 전형단계 (Pipeline)
구조는 **[고정] 접수 → [자유 추가 영역] → [고정] 처우 협의 → [고정] 최종합격 → [고정] 탈락(=목록/필터에서는 "최종탈락"으로 표기)**

- **접수**와 **처우 협의** 사이에 원하는 만큼 커스텀 단계를 자유롭게 추가할 수 있고, 드래그 핸들(⠿)로 순서 재배열 가능
- "+ 전형단계 추가" 클릭 → 검색 또는 새 이름 입력 → **"OO 새 전형단계 추가"** 선택 시 상세 설정 모달이 뜸:

**전형 단계 추가하기 모달**
| 필드 | 설명 |
|---|---|
| 색상 | 18가지 컬러 팔레트에서 단계 라벨 색 선택 |
| 유형 | **서류 / 인터뷰** 2가지 중 선택 (뒤에 이어지는 세부 필드가 달라짐) |
| 전형 단계 이름 | 텍스트 (검색창에 입력한 값이 자동으로 채워짐) |
| 설명 | 선택 입력 |
| 공고 생성 시 자동 추가 | 체크박스 — 켜두면 앞으로 새 공고를 만들 때마다 이 단계가 기본으로 포함됨 |

안내 문구: "전형단계 관리 목록에 추가돼요." → **회사 전체에서 재사용되는 전형단계 마스터 목록**에 등록됨(공고마다 새로 만들 필요 없이 검색해서 재사용 가능)

**단계 카드에 붙는 하위 설정 필드** (단계 유형에 따라 다름)
- 공통: **평가 템플릿**(검색 또는 새 템플릿 추가, "템플릿 관리" 링크) / **평가자**(조직 구성원 중 다중 선택, "채용 계정 목록"에서 검색)
- **인터뷰** 유형에만 추가로: **진행 시간**(면접 소요시간) / **최소 평가자 인원**
- 각 단계 카드 우상단 "···" 메뉴: **설정 복사하기**(다른 단계로) / **여기에 붙여넣기** — 평가템플릿·평가자 설정을 단계 간 복붙 가능
- 삭제(🗑) 아이콘으로 단계 제거 가능

고정 단계(접수/처우 협의/최종합격/탈락)는 삭제・이름변경 불가, 위치도 고정.

#### 탭 3. 지원정보
세 블록으로 구성:

**① 지원 제출자료**
- 안내: "후보자가 지원 시 제출할 자료를 설정할 수 있어요."
- "+ 추가"로 행을 계속 늘릴 수 있음. 각 행: **제출 자료 이름**(자유 텍스트, 예: 이력서/포트폴리오/자기소개서) + **필수 제출 / 선택 제출** 드롭다운
- 드롭다운 안에는 "제출 여부"(필수 제출 ✓ / 선택 제출) 구획과 별도로 **"제출 방식" → 파일로만 제출 가능** 토글이 있어, 링크 제출을 막고 파일 업로드만 허용하도록 강제할 수 있음
- 빈 행은 우측 빨간 ⊗ 아이콘으로 삭제

**② 사전 질문**
- 안내 아이콘(ⓘ) 툴팁: "후보자는 최대 10,000자까지 작성할 수 있어요."
- "+ 추가"로 질문 행 추가. 각 행: **질문 작성**(자유 텍스트) + **필수 / 선택** 드롭다운(제출자료보다 단순 — 파일 전용 옵션 없음)

**③ 추가정보**
- 안내: "후보자가 추가적으로 작성해야 할 정보를 설정할 수 있어요."
- 체크박스 5개, 체크 시에만 옆의 **필수/선택** 토글이 활성화됨:
  - **학력정보** (필수/선택 선택 가능)
  - **경력정보** (필수/선택 선택 가능)
  - **병역정보** (필수/선택 선택 가능)
  - **보훈대상** (체크박스만 있음, 필수/선택 구분 없음 — 항상 자율 기재)
  - **장애대상** (체크박스만 있음, 필수/선택 구분 없음 — 항상 자율 기재)

저장 버튼 "공고 저장하기" → 토스트 "공고를 만들었어요." → 목록에 카드로 즉시 반영.

---

## 3. 사이트(채용 사이트) 탭

- 서브탭 "채용사이트" 하나
- 사이트 제목(회사명 + "채용", 인라인 편집 가능) + "사이트 주소 정하기" 링크 + 우상단 공개/비공개 토글
- "사이트 목록 (0)" — 채용 사이트를 여러 개 만들어 관리 가능(+추가하기)
- 사이트가 없을 때 프로모션 영역: **"유명 테크기업 수준의 채용 사이트를 30초 만에 만들어보세요."** + "AI로 사이트 만들기" 버튼(회사 특징 키워드만 입력하면 자동 생성, 모바일 뷰도 함께 생성) + 기본 템플릿 미리보기(회사명 자동 삽입, 채용 공고 카드 리스트 노출)
- 여기서 공개로 설정한 "직군" 단위 필터가 후보자에게 노출되는 구조 (공고 탭에서 본 "직군" 칩과 연결)

---

## 4. 전체 데이터 연결 요약

```
[공고] 직무·직군 태그 설정 ─┬─▶ [사이트] 공개 채용페이지에 직군별로 노출
                        └─▶ [후보자] 필터의 직무/직군 옵션으로 사용
                        (직무는 "구성원" 인사 마스터 데이터와도 공유됨)

[공고] 전형단계(접수→커스텀→처우협의→최종합격/탈락) + 평가템플릿/평가자 설정
   └─▶ [후보자] 등록 시 이 공고를 선택하면
         · 지원 제출자료/사전질문/추가정보 입력폼이 그대로 뜨고
         · 접수 단계에서 시작해 "다음으로" 버튼으로 파이프라인을 그대로 통과하며
         · "평가" 탭에 단계별 평가자 지정/인터뷰 일정 체크리스트가 자동 생성됨
```

---

## 참고
- 이 문서의 모든 필드·옵션·문구는 실제 계정에서 "프론트엔드 개발자 모집" 공고를 만들고 후보자 "홍김동"을 등록·이동시키며 화면에 나타난 그대로 기록한 것입니다.
- 채용 관련 개인정보(실제 이력서 내용 등)는 테스트용 가상 데이터(hong@example.com 등)만 사용했으며, 실제 개인정보를 기록하지 않았습니다.
