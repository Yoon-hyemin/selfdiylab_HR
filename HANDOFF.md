# 세션 인수인계 — 2026-08-28 (4차, 최종 갱신)

이 문서는 컨텍스트가 꽉 찼거나 세션이 끊긴 뒤 새 세션이 이어받기 위한 것이다. **먼저 `CLAUDE.md`의 "인재검색 자동화" 절(특히 맨 아래 — "리스트 가져오기에 Level1 자동필터 + 자동 페이지 넘김")을 읽으면 이 기능의 현재 상태를 대부분 파악할 수 있다.** 이 문서는 그걸 보완하는 세션 진행 상태·다음 행동 요약이다.

**중요 — 워크트리 혼동 주의**: 이 세션은 원래 다른(오래된) 워크트리(`claude/talent-search-automation-4-...`, 브랜치가 Phase 1B-4c에서 멈춰 있던 것)에서 시작했다가, HANDOFF.md를 확인하고 실제 최신 작업이 있는 이 워크트리(`talent-search-automation-fff2d5`, 브랜치 `claude/talent-search-automation-6d8ced`)로 `EnterWorktree`(path 지정)를 통해 옮겨왔다. 여러 워크트리가 인재검색 관련 브랜치를 갖고 있으니, 다음 세션도 시작할 때 **반드시 이 파일이 있는 워크트리·브랜치가 맞는지 먼저 확인할 것**(`git log --oneline -5`로 최근 커밋이 이 문서에 적힌 것과 일치하는지 확인).

## 1. 지금 어디까지 왔나

인재검색 자동화 기능이 Phase 1A → 1B → 1C → 1D → 1E까지 웹앱 쪽 전부 완료, 크롬 확장 실행엔진(OCR MVP + 실제 후보 리스트 가져오기)까지 완료된 상태에서 이번 세션을 시작했다(전부 CLAUDE.md에 기록됨, 여기서 반복 안 함).

**이번 세션에 새로 완료한 것**: "실제 후보 리스트 가져오기"에 두 가지를 추가했다 — (1) 저장 시점 Level1 자동필터(이력서 업데이트일 + 경력연수, 명확히 조건 밖인 후보는 아예 저장 안 함), (2) 크롬 확장의 자동 페이지 넘김(목표 인원 채울 때까지 안전장치를 두고 여러 페이지 자동 수집). 설계→계획→서브에이전트 구현(4개 태스크)→태스크별 리뷰→**실제 크롬으로 라이브 검증**→최종 전체 리뷰→fix wave까지 전부 끝났다.

**라이브 검증 중 실제로 잡은 버그 2개**(전부 4개 태스크 개별 코드리뷰는 통과한 뒤에야 실사용 중 발견됨 — CLAUDE.md에 상세 기록):
1. `findNextPageButton`이 텍스트("다음")만으로 찾다가 같은 화면 위쪽 "스페셜 태그" 캐러셀의 다음 버튼을 잘못 집던 문제 → `.PageBox .BtnNext`로 범위 좁혀 해결.
2. Postgres `numeric` 컬럼이 문자열로 내려오는데 `"5" + 0.5`가 JS에서 문자열 이어붙이기가 돼서 경력연수 상한 필터가 사실상 무력화돼 있던 버그 → `Number()` 명시 변환으로 해결(핸들러+순수함수 이중 방어, `project_detail_out`도 같이 고침).

두 버그 다 고친 뒤 실제 사람인 검색결과로 목표 인원(10) 채우기를 다시 실행해서, 2페이지에 걸쳐 정확히 경력범위 안인 후보 13명만 저장되는 것까지 확인 완료.

**같은 세션에서 이어서 완료한 두 번째 슬라이스**: 사용자가 "1차(Level1 필터) → 2차(점수 매기기) → 정제된 인원만 검토"를 요청해서, "실제 후보 리스트"에 직무 60점 자동채점을 추가했다. 공통 40점은 리스트 데이터에 근거가 없어서 제외하고 직무 60점만 60점 만점으로 적용, 근거수준은 태그·경력요약이 프로젝트 키워드와 겹치는 정도로 단일 추정, 기준(42/60) 통과자만 기본 노출·미달자는 더보기. 설계→계획→서브에이전트 구현(3개 태스크)→태스크별 리뷰(1건 fix)→최종 전체 리뷰(2건 fix)까지 완료. **문서화까지 끝낸 뒤 컨트롤러가 직접 Claude in Chrome으로 실제 브라우저에 로그인해서 시각 확인도 마쳤다** — "2026.08 인플루언서 마케터" 프로젝트에 근거수준 4단계(없음/약함/부분/명확)를 각각 대표하는 더미 후보 4명을 DB에 직접 넣고, 표에 "근거(예: 명확 4/4)"·"직무60점"·"판정" 칼럼이 정확히 뜨는지, 기본으로 추천 2명만 보이고 "확인 필요 2명 더보기"를 누르면 나머지가 펼쳐지는지까지 눈으로 확인함(테스트 데이터는 확인 직후 삭제). 레이아웃도 깨짐 없이 정상.

브랜치: `claude/talent-search-automation-6d8ced` (worktree, `master`에 아직 머지 안 함). `git status` 클린.

**커밋 순서** (플랜 커밋 `f3987b4` 다음부터):
- Level1 필터+자동페이지넘김: `7d6e54a`(Task1 필터 순수함수) → `cff3deb`(Task2 저장API 통합) → `7e3f41a`(Task3 다음페이지 탐색) → `40ff852`(Task4 팝업 루프) → `c11297f`(Task4 fix round: 0명 메시지) → `dfd4f85`(라이브검증 fix: 다음버튼 선택자) → `da25f27`(라이브검증 fix: 경력연수 문자열버그) → `482785a`(최종리뷰 fix wave: 4건) → `4606f60`(CLAUDE.md/HANDOFF.md 문서화)
- 직무60점 자동채점: `c576eef`(Task1 정책로딩 버그수정) → `9b0fd1f`(Task2 채점 순수함수) → `7b32de7`(Task3 표 반영) → `2bc995c`(Task3 fix round: 안내문구) → `e6b5ee1`(최종리뷰 fix wave: 근거표시+키워드없음 안내)

## 2. 작업 방식 (이 세션 내내 유지됨 — 새 세션도 그대로 따를 것)

brainstorming → writing-plans → subagent-driven-development 3단계, 이전 세션들과 동일. 이번엔 특히:

- 사용자가 대화 중간에 방향을 뒤집은 사례가 있었다 — 처음엔 "가져온 뒤 화면에서 걸러내기"로 설계했다가, 사용자가 "애초에 가져오는것부터 통과해야 가져올 수 있게" 요청해서 저장 시점 필터로 설계를 바꿨다. 그 직후 "여러 페이지 자동 순회"도 사용자가 먼저 물어봐서(AskUserQuestion으로 방식 3개 중 선택받음) 같은 슬라이스에 편입시켰다. **설계 확정 전 대화라도 사용자가 방향을 바꾸면 바로 반영하고, 이미 쓴 설계문서도 그 자리에서 고쳐서 일관성을 유지할 것.**
- **최종 리뷰(opus)가 실제로 유용한 발견을 했다**: 4개 태스크 개별 리뷰는 다 통과했는데도, 전체 diff를 다시 보는 최종 리뷰가 "numeric 컬럼이 문자열로 오는 문제가 이 diff 다른 곳에도 있는지" 같은 교차 점검 질문을 던져서 `project_detail_out`의 같은 버그를 추가로 찾아냈다. 개별 태스크 리뷰만으로 끝내지 말고 최종 전체 리뷰를 반드시 거칠 것.
- **크롬 확장 작업은 여전히 라이브 검증 없이 "완료"로 치면 안 된다** — 이번에도 코드리뷰를 통과한 코드에서 실사용 중에만 발견되는 버그가 2개 나왔다(위 1절 참고). 서브에이전트는 브라우저 접근이 없어 이 검증을 못 하므로, 컨트롤러가 `Claude in Chrome`(mcp__claude-in-chrome__*) 도구로 사용자의 실제 크롬에 연결해서 직접 진행해야 한다.
- **`Claude in Chrome` 연결 시 주의할 점**(이번 세션에 새로 겪은 것들):
  - 여러 브라우저가 연결돼 있으면 `list_connected_browsers`가 표시명 없이 "Browser 1/2"만 보여준다 — 사용자에게 어느 것인지 물어봐야 하는데, 사용자가 모르면 `switch_browser`로 확인창을 각 크롬에 띄워서 직접 고르게 할 수 있다(단, 그 확인창이 안 뜨면 사용자가 Chrome/확장이 켜져 있는지 먼저 확인해야 함).
  - **native `confirm()`/`alert()` 다이얼로그는 `computer` 도구의 클릭·키 입력으로 절대 조작 못 한다** — 페이지 자체가 멈춘 것처럼 보이고(`Script injection timed out`, CDP 타임아웃) 스크린샷도 안 찍힌다. 이 경우 사용자에게 직접 눌러달라고 요청하는 것 외에 방법이 없다. 컴퓨터 확장 아이콘 클릭(툴바 UI)도 페이지 DOM 밖이라 마찬가지로 자동화 불가 — 사용자가 직접 눌러야 한다.
  - **로그인 비밀번호 입력은 안전 규칙상 절대 대신 하면 안 된다**(실제 계정이든 테스트 계정이든 동일) — 이메일 칸은 채워줘도 되지만 비밀번호 칸은 사용자가 직접 입력하게 요청할 것.
  - **`preview_start`(Browser pane)로 띄운 dev 서버가 엉뚱한 워크트리 경로에서 실행될 수 있다** — 이번 세션에서 실제로 겪음(`.claude/launch.json` 설정이 세션 시작 시점 워크트리에 묶여 있어서, `EnterWorktree`로 워크트리를 옮긴 뒤에도 예전 워크트리 경로로 서버가 뜸 → `ERR_MODULE_NOT_FOUND` 500 에러). `preview_logs`로 에러 메시지의 파일 경로를 확인해서 워크트리가 맞는지 검증할 것 — 안 맞으면 `preview_stop` 후 Bash로 직접 `node scripts/dev-server.js`를 현재 워크트리 경로에서 백그라운드 실행하는 게 안전하다.
  - **사람인 같은 실제 채용 플랫폼 대상으로 자동화된 반복 클릭(빠른 루프)을 컨트롤러가 직접 실행하지 말 것** — 이번 세션에서 "마지막 페이지 상태를 확인하려고" 90번 연속 클릭(900ms 간격)을 JS로 돌렸다가 화면이 잠깐 로딩 스켈레톤 상태로 멈추는 걸 보고 중단했다(다행히 차단까지는 안 갔지만 위험한 행동이었다). 확장 자체의 안전장치(최대 5페이지, 2.5~4.5초 무작위 지연)를 우회해서 더 빠르게/많이 돌리는 검증은 하지 말 것 — 필요한 검증은 정상 흐름(확장 UI를 통한 느린 반복) 안에서만 하고, 정 궁금하면 읽기 전용 DOM 조회 한두 번으로 끝낼 것.

## 3. 알려진 미해결 사항

- **프로덕션 마이그레이션 미반영**: `sql/018`~`021`이 여전히 `development` 브랜치에만 적용돼 있다(이번 세션은 새 마이그레이션을 추가하지 않아서 이 상태 자체는 안 바뀜). `master` 배포 전 사용자 확인 후 반영 필요.
- **크롬 확장의 `HR_SITE_ORIGIN`이 여전히 `localhost:3000` 하드코딩** — 배포 시 실제 도메인으로 바꿔야 함(아직 안 함).
- **최종 리뷰가 남긴 Minor 항목들**(전부 의도적으로 이번 범위에서 안 고침, `.superpowers/sdd/2026-08-28-talent-search-list-import-level1-filter/progress.md`에 기록):
  - `parseCareerYears`가 "신입"을 숫자 정규식보다 먼저 체크해서, "신입·경력 3년" 같은 드문 표기는 0년으로 오판할 수 있음(실사용에서 "경력 N년 M개월" 형식만 관찰됨, 이론상 리스크).
  - 페이지 넘김 루프 전체가 크롬 확장 팝업의 생명주기 안에 있어서, 팝업이 포커스를 잃으면(다른 창 클릭 등) 중간에 죽을 수 있음 — service worker로 옮기는 게 근본 해결책이나 별도 슬라이스.
  - `resumeAgeDays` 계산이 서버 UTC와 사람인 라벨(KST 기준으로 보이는 `YY-MM-DD`)의 기준 시각이 미묘하게 달라서 정확히 180/181일 경계에서 하루 오차 가능.
- **"5페이지 안전 상한"과 "마지막 페이지 도달" 경로는 이번 세션에 라이브로 직접 확인하지 못했다** — 목표 인원(10)이 1~2페이지 안에서 채워지는 시나리오만 실제로 검증됐다. 코드 리뷰(Task4 리뷰어의 손 추적 검증)로는 논리적으로 맞다고 확인했지만, 다음에 시간이 되면 목표 인원을 아주 크게(예: 500) 설정해서 5페이지에서 정말 멈추는지, 검색 결과가 적은 검색어로 진짜 마지막 페이지에서 "마지막 페이지예요"로 정상 종료하는지 실제로 한 번 더 확인해볼 것.
- 재직여부 필터 없음(설계 당시 의도적 범위 제외 — 이 시스템에 그런 조건 필드 자체가 없음).
- 직무60점 채점의 알려진 한계(전부 최종 리뷰에서 발견, 의도적으로 이번 범위 밖 — CLAUDE.md에 상세 기록): 가상 후보 화면과 이 화면이 같은 '추천' 배지를 쓰는데 판정 기준이 완전히 다름(라벨 분리 고려 여지), `tsListSortDir`가 프로젝트를 옮겨도 초기화 안 됨, 정책 버전이 50개 넘게 쌓이면 오래된 프로젝트의 고정 버전을 못 찾아 이 화면 채점만 조용히 꺼질 수 있음.

## 4. 다음에 할 일

직무60점 자동채점은 코드·라이브 검증까지 끝났다(위 1절 참고). 다음 논의 주제는 명시적으로 확정된 게 없다. 원본 명세(`인재검색_자동화_마스터프롬프트_원본.md`) 기준 아직 안 만든 것: 여러 페이지에 걸친 중복 후보 처리(서버 쪽 영구 중복제거), 사람인 외 플랫폼(잡코리아/원티드/리멤버), 제외된 후보를 기록해서 나중에 "왜 걸렀는지" 확인하는 기능(감사로그), 재직여부 필터. 전부 사용자와 스코프 재논의 필요 — **새 세션이 이 방향을 기본값으로 강행하지 말고 짧게 확인부터 할 것.**

## 5. 사용자와의 협업 방식 (중요 — 여러 세션에 걸쳐 반복 확인됨)

- **"그만 묻고 빨리 해줘"류 피드백을 여러 번 받았다.** 매 슬라이스마다 진행 여부는 짧게 한 번만 확인하고, 승인되면 설계→계획→구현→검증까지 중간에 재확인 없이 쭉 진행할 것.
- **이번 세션에 새로 확인된 것**: 라이브 검증 중 브라우저 native 다이얼로그 때문에 사용자에게 여러 번 클릭을 요청해야 했는데, 그때마다 사용자가 눈에 띄게 피로해했다("아우 진짜", "니가 알아서하라니까"). **사용자에게 뭔가 클릭해달라고 요청할 땐 한 번에 정확히 뭘 눌러야 하는지 명확히 하고, 정말 필요한 것만 최소 횟수로 요청할 것** — 특히 native 다이얼로그(확장 리로드, confirm 팝업, 확장 아이콘 클릭)처럼 자동화 도구가 원천적으로 접근 못 하는 지점만 부탁하고, 나머지(로그인 후 페이지 이동, 결과 확인 등)는 최대한 컨트롤러가 직접 처리할 것.
- **또 하나 확인된 것**: 컨트롤러가 테스트용으로 아무 프로젝트나 골라서 검증하면, 사용자 입장에서는 "내가 설정한 조건과 다른 결과가 나왔다"고 혼란스러워할 수 있다(실제로 발생함 — 인플루언서 마케터 프로젝트에 영상편집 검색 결과를 넣어서 테스트했다가 지적받음). **테스트 데이터를 실제 프로젝트에 섞어 넣을 때는 그게 테스트라는 걸 미리 알리거나, 애초에 실제 프로젝트를 쓰지 말고 사용자가 지금 하려는 작업과 일치하는 데이터로만 검증할 것.**
- **AskUserQuestion 도구(선택지 버튼)는 이번 세션 대부분 정상 작동했다** — 페이지네이션 방식, 직무60점 채점에서 공통40점 처리 방식 등 선택에 성공적으로 씀. 단 한 번 입력 JSON이 깨져서(한글 이스케이프+수동 조립 시도 중 발생) 에러가 났었다 — 도구 자체의 파라미터는 항상 정상적인 함수 호출 방식으로 채우고, 문자열을 직접 손으로 이어붙이지 말 것.
- **한 슬라이스가 끝난 뒤 사용자가 바로 다음 요청을 새로 던지는 패턴이 반복됐다** — "인수인계 문서 갱신 중이다"라고 설명해도 사용자는 그걸 "세션을 끝내려는 신호"로 오해하고 "다음세션으로 넘기라고?"처럼 되물었다. **문서 정리는 세션을 끝내는 게 아니라 매번 하는 습관이라는 걸 명확히 하고, 사용자가 새 요청을 던지면(설령 방금 한 슬라이스의 마무리 작업 중이라도) 즉시 그 요청으로 전환할 것** — 이번엔 브레인스토밍을 새로 시작해서 처리함.
- 설명은 최대한 짧게, 개조식으로. 표/헤더보다 3~5줄 요약을 선호.
- **비개발자다** — 브라우저 자동화 도구가 뭘 할 수 있고 없는지 여러 번 설명이 필요했다(특히 이번엔 native 다이얼로그·확장 아이콘 클릭이 새로운 유형의 "제가 못 하는 것"이었다). 무엇을, 어디를, 어떤 순서로 클릭할지 매우 구체적으로 안내해야 헤매지 않는다.
