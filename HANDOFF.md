# 세션 인수인계 — 2026-08-27 (3차, 최종)

이 문서는 컨텍스트가 꽉 찼거나 세션이 끊긴 뒤 새 세션이 이어받기 위한 것이다. **먼저 `CLAUDE.md`의 "인재검색 자동화" 절(특히 맨 아래 두 개 — 실행엔진 OCR MVP, 사람인 실제 후보 리스트 가져오기)을 읽으면 이 기능의 현재 상태를 대부분 파악할 수 있다.** 이 문서는 그걸 보완하는 세션 진행 상태·다음 행동 요약이다.

## 1. 지금 어디까지 왔나

인재검색 자동화 기능이 Phase 1A → 1B(1B-1~1B-4c) → 1C → 1D(1D-1, 1D-2) → 1E(1E-1, 1E-2, 1E-3)까지 웹앱 쪽은 전부 완료됐고, 그 뒤 같은 세션에서 **완전히 새로운 컴포넌트인 크롬 확장("실행엔진")도 두 단계 만들어서 실제로 끝까지 검증까지 마쳤다**:

- **웹앱(1A~1E-3)**: 화면 골격, 권한, 채점 기준 관리센터, 검색 프로젝트 생성·승인, 가상 후보 시뮬레이션 채점, 대시보드 누적 추천 연동 — 전부 완성(자세한 건 CLAUDE.md 참고, 여기서 반복 안 함).
- **크롬 확장 1단계 — 실행엔진 OCR MVP**: 사람인 후보 상세 이력서 화면을 스크롤하며 캡처 + 로컬 OCR(Tesseract.js)로 텍스트 추출. 실제 후보로 검증 완료.
- **크롬 확장 2단계 — 실제 후보 리스트 가져오기**: 사람인 검색결과 리스트(한 화면 ~20명)를 확장이 통째로 읽어서, 연결 코드로 인증된 HR 사이트 API를 통해 "검색 진행" 화면의 새 섹션("실제 후보 리스트")에 채점 없이 표시. **오늘 이 세션에서 실제 로그인 세션으로 라이브 검증까지 끝까지 마쳤다** — 실제 후보 20명을 실제로 가져와서 이름·나이·경력·학력·태그·특징배지·최종업데이트일·원문링크가 DB와 화면에 정확히 반영되는 것까지 확인함.

브랜치: `claude/talent-search-automation-6d8ced` (worktree, `master`에 아직 머지 안 함). `git status` 클린.

**라이브 검증 중 실제로 잡은 버그 3개** (전부 코드리뷰 단계에서는 안 잡히고, 실제 브라우저에 확장을 로드해서 써보고서야 발견됨 — 아래 "작업 방식" 절 마지막 항목 참고):
1. 사람인 검색리스트 페이지의 DOM 선택자가 미검증 placeholder였던 것 → 실제 로그인해서 라이브 DOM 확인 후 전부 실제 값으로 교체(`.talent_list_item`, `.check_area[residx]` 등 — 자세한 선택자 목록은 CLAUDE.md 참고).
2. `manifest.json`에 `"storage"` 권한이 빠져있어서 `chrome.storage.local`(연결 코드 저장)이 조용히 실패 → 권한 추가.
3. `index.html`의 `renderTalentSearchCandidatesScreen`에서 가상 후보가 하나도 없는 프로젝트는 early-return 때문에 새로 추가한 "실제 후보 리스트" 섹션 자체가 영영 안 뜨는 버그 → 세 분기 모두 공통으로 그 섹션을 렌더링하도록 수정.

이 세 개를 고친 뒤 실제로 20명 가져오기 end-to-end 성공, 테스트로 만든 프로젝트/후보 데이터는 확인 직후 정리함(DB에 안 남아있음).

## 2. 작업 방식 (이 세션 내내 유지됨 — 새 세션도 그대로 따를 것)

매 서브 단계마다: **brainstorming 스킬(설계 문서 → `docs/superpowers/specs/`) → writing-plans 스킬(구현계획 → `docs/superpowers/plans/`) → subagent-driven-development 스킬(Task별 fresh subagent 구현 + task review + 최종 whole-branch review + fix wave + scoped re-review)** 순서를 반복해왔다.

- 최종 review는 opus, task review는 sonnet, 구현은 haiku(단순)~sonnet(통합/판단 필요)로 모델을 나눠 씀.
- 서브에이전트가 `.env.local`이 가리키는 **development** Neon 브랜치에서 실제로 curl로 검증. **프로덕션 브랜치는 절대 건드리지 않는다.**
- 서브에이전트가 만든 테스트용 검색 프로젝트/후보 데이터는 매번 세션 끝에 컨트롤러가 직접 SQL로 정리해왔다 — 새 세션도 작업 끝날 때 `talent_search_projects`에 "테스트"류 제목이 남아있는지 확인하고 지울 것.
- **중요 — 크롬 확장 작업의 교훈**: 코드 리뷰(task review, 최종 whole-branch review, fix wave 재검토까지 전부 거쳐도)만으로는 못 잡는 버그가 실제로 있었다(위 1번 항목의 DOM 선택자, 2번 manifest 권한 누락, 3번 렌더링 분기 버그 — 셋 다 여러 단계 리뷰를 통과한 뒤에야 실사용 중 발견됨). **크롬 확장 관련 작업은 코드 리뷰가 끝났다고 "완료"로 치지 말고, 실제 크롬에 로드해서 실제 로그인 세션으로 한 번은 반드시 끝까지 돌려봐야 한다.** 이 실사용 검증은 서브에이전트가 못 하므로(샌드박스라 브라우저 접근 불가) 컨트롤러(메인 세션)가 사용자와 함께 직접 진행해야 한다 — `chrome://extensions` 페이지 접근과 "압축해제된 확장 프로그램 로드"는 브라우저 자동화 도구로 근본적으로 접근이 막혀 있어서(의도된 보안 경계), 확장 로드·리로드 클릭은 매번 사용자가 직접 해야 한다.

## 3. 알려진 미해결 사항

- **프로덕션 마이그레이션 미반영**: `sql/018`~`sql/021`이 아직 `development` 브랜치에만 적용돼 있다(`sql/015`~`017`은 2026-08-26에 이미 production 반영 완료 — CLAUDE.md Phase 1C 절 참고). `master`에 머지해서 배포하려면 이 네 파일을 먼저 프로덕션 브랜치 connection string으로 실행해야 한다(사용자 확인 후).
- **크롬 확장의 `HR_SITE_ORIGIN`이 `http://localhost:3000`으로 하드코딩돼 있다**(`chrome-extension/popup.js`) — 배포 시 실제 도메인으로 바꿔야 한다는 걸 코드 주석에 남겨뒀지만, 실제로 바꾸는 작업은 아직 안 함.
- **`GET /talent-search-policy/versions`의 50개 제한** — 이전부터 있던 이슈, 아직 안 고침(CLAUDE.md 참고).
- **"오늘 추천"은 여전히 항상 0** — 하루 단위 배치 개념이 없어서, 의도적으로 그대로 둠.
- **사용자 피드백(중요, 2026-08-27 라이브 검증 직후)**: "실제 후보 리스트 가져오기"를 실사용해본 뒤 "우리 회사 조건에 맞는 사람만 뽑아오는 게 아니라 화면에 보이는 걸 그냥 그대로 복사해오는 것뿐이라 번거롭다"는 피드백을 받았다. 정확한 지적이다 — 이 기능은 설계 당시 의도적으로 채점을 범위 밖에 뒀는데(리스트 정보만으론 경력공백 등을 정확히 판단 못 해서), 실사용해보니 이게 체감 가치를 떨어뜨린다는 게 확인됐다. **다음 세션에서 제일 먼저 다시 논의할 주제**: 리스트에서 이미 나오는 정보(경력연수, 최종업데이트일 등)만으로라도 Level1 스타일 객관적 자동 필터를 적용해서 최소한의 자동 거르기를 넣는 방향(사용자에게 이렇게 제안했었고, 아직 착수 전 상태에서 세션이 끝남).

## 4. 다음에 할 일

**바로 위 3번 항목(리스트 후보에 최소한의 자동 필터 적용)이 사용자가 세션 종료 직전 가장 최근에 논의하던 주제다 — 새 세션은 이걸 먼저 사용자와 확인하는 게 자연스럽다.** 다만 아래처럼 명시적으로 다시 확정된 건 아니므로, 새 세션이 이 방향을 기본값으로 강행하지 말고 짧게 확인부터 할 것(단, 아래 5번 항목 참고 — 확인은 한 번만, 길게 여러 개 물어보지 말 것).

그 외 원본 명세(`인재검색_자동화_마스터프롬프트_원본.md`) 기준 아직 안 만든 것: 여러 페이지 자동 순회, 중복 후보 처리, 사람인 외 플랫폼(잡코리아/원티드/리멤버), 일시정지·체크포인트·배치 스케줄링 등(1E 항목). 전부 사용자와 스코프 재논의 필요.

## 5. 사용자와의 협업 방식 (중요 — 여러 세션에 걸쳐 반복 확인됨)

- **"그만 묻고 빨리 해줘" 류 피드백을 여러 번 받았다.** 매 슬라이스마다 진행 여부는 짧게 한 번만 확인하고, 승인되면 설계→계획→구현→검증까지 중간에 재확인 없이 쭉 진행할 것.
- **"오늘은 여기까지 할까요/계속할까요?" 같은 질문 자체를 이 세션에서 명시적으로 하지 말라는 피드백을 받았다** — "그런 질문 안 했으면 좋겠고, 새 세션으로 넘어갈 수 있게 정리부터 해달라"고 직접 말함. 세션을 마무리할 때는 계속할지 묻지 말고 바로 인수인계 문서 정리로 들어갈 것.
- **AskUserQuestion 도구(선택지 버튼)가 이 세션에서 응답을 못 받은 적이 있었다** — 반복 실패하면 평문 텍스트로 짧게 질문하고 다음 메시지를 기다리는 쪽으로 전환할 것.
- 설명은 최대한 짧게, 개조식으로. 표/헤더보다 3~5줄 요약을 선호.
- **비개발자다** — 브라우저 자동화 도구가 뭘 할 수 있고 없는지(예: `chrome://` 페이지·확장 로드·네이티브 파일선택창·비밀번호 입력은 전부 접근 불가) 여러 번 설명이 필요했다. 크롬 확장 실사용 검증을 같이 할 때는 "무엇을, 어디를, 어떤 순서로 클릭할지"를 매우 구체적으로(탭 이름, 버튼 위치 등) 안내해야 헤매지 않는다.
