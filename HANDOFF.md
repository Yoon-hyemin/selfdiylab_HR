# 세션 인수인계 — 2026-08-27

이 문서는 컨텍스트가 꽉 찼거나 세션이 끊긴 뒤 새 세션이 이어받기 위한 것이다. 이전(2026-08-19) 인수인계 문서는 Phase 1B-2 시점 기준이라 지금은 완전히 낡았다 — 이 문서로 전부 대체한다. **먼저 `CLAUDE.md`의 "인재검색 자동화" 절을 읽으면 이 기능의 현재 상태를 대부분 파악할 수 있다.** 이 문서는 그걸 보완하는 세션 진행 상태·다음 행동 요약이다.

## 1. 지금 어디까지 왔나

인재검색 자동화 기능이 Phase 1A → 1B(1B-1~1B-4c) → 1C → 1D(1D-1, 1D-2) → 1E(1E-1, 1E-2, 1E-3)까지 전부 완료됐다. 요약하면:

- **1A~1B**: 화면 골격, 권한(`can_use_talent_search`), 채점 기준 관리센터(1차필터/공통40점/직무60점/근거수준/임계값, 초안·적용·버전이력·복구, 가상후보 3명 미리보기) — 전부 완성.
- **1C**: "+ 새 인재검색"으로 실제 검색 프로젝트를 만들 수 있다(키워드 5종, 상세조건, 추가질문 시뮬레이션, 직무템플릿).
- **1D-1**: 프로젝트 목록/상세 조회 + 대시보드 실제 연동 + 검토 화면(요약/조건/채점기준 표시).
- **1D-2**: "이 조건으로 검색" 승인 버튼 — 누르면 상태가 draft→approved로 바뀌고 그 시점 채점 기준이 프로젝트에 영구히 고정된다(`policyVersionId`). 플랫폼별 검색어(규칙 기반, 실제 문법 차이는 아직 없음)도 이때 추가.
- **1E-1**: 승인된 프로젝트에서 "검색 진행 보기" → 가상 후보(최소 100명, 목표인원 비례) 생성 → 1B-4c 채점 엔진(수정 없이 재사용)으로 추천/확인필요/제외 판정.
- **1E-2**: 후보 행 클릭 → 상세 모달(raw 속성 + 채점 세부) → "정보 부족"/"중복" 수동 표시(자동판정보다 우선).
- **1E-3**: 대시보드 카드의 "누적 추천" 숫자가 실제 값으로 계산됨(전엔 항상 0 하드코딩). "오늘 추천"은 여전히 0 — 하루 단위 배치 개념 자체가 아직 없어서 의도적으로 그대로 둠.

브랜치: `claude/talent-search-automation-6d8ced` (worktree, `master`에 아직 머지 안 함 — 사용자가 "지금은 다음 단계 진행"을 매번 선택해왔음). `git status` 클린, 최신 커밋 `a4be354`.

이 문서는 웹앱(1A~1E) 브랜치 기준이다 — 이후 별도로 `chrome-extension/`(Manifest V3, 사람인 이력서 OCR 추출 실행엔진 MVP)이 추가됐다. 배경/설치 방법은 CLAUDE.md의 "실행엔진" 절, 설계는 `docs/superpowers/specs/2026-08-27-talent-search-execution-engine-ocr-mvp-design.md`, 구현계획은 `docs/superpowers/plans/2026-08-27-talent-search-execution-engine-ocr-mvp.md` 참고.

## 2. 작업 방식 (이 세션 내내 그대로 유지됨)

매 서브 단계마다: **brainstorming 스킬(설계 문서 → `docs/superpowers/specs/`) → writing-plans 스킬(구현계획 → `docs/superpowers/plans/`) → subagent-driven-development 스킬(Task별 fresh subagent 구현 + task review + 최종 whole-branch review + fix wave + scoped re-review)** 순서를 반복해왔다. 새 세션도 이 패턴을 그대로 따르면 된다.

- 최종 review는 opus, task review는 sonnet(가끔 haiku, 아주 단순한 마이그레이션류), 구현은 haiku(코드가 계획에 이미 다 있는 단순 작업)~sonnet(통합/판단 필요한 작업)으로 모델을 나눠 써왔다.
- 매 슬라이스 끝날 때마다 서브에이전트가 `.env.local`이 가리키는 **development** Neon 브랜치에서 실제로 curl/브라우저로 검증했다. **프로덕션 브랜치는 절대 건드리지 않는다** — 반영은 항상 사용자 확인 후 별도로.
- 서브에이전트들이 수동 확인 과정에서 만든 테스트용 검색 프로젝트/후보 데이터는 매번 세션 끝에 컨트롤러(나)가 직접 SQL로 정리해왔다 — 새 세션도 작업 끝날 때 `talent_search_projects`에 "검증용"/"테스트" 류 제목이 남아있는지 확인하고 지울 것.
- 최종 review에서 나온 finding은 Critical/Important는 반드시 fix wave(한 번에 묶어서) + scoped re-review까지 거치고, Minor는 ledger에 기록만 하고 넘어간다.

## 3. 알려진 미해결 사항 (확인됨)

- **프로덕션 마이그레이션 미반영**: `sql/018`(1D-2), `sql/019`(1E-1), `sql/020`(1E-2)이 아직 `development` 브랜치에만 적용돼 있다. `sql/015`~`017`은 이미 production에 반영 완료(2026-08-26에 확인·반영함 — CLAUDE.md Phase 1C 절 참고). `master`에 머지해서 배포하려면 이 세 파일을 반드시 먼저 프로덕션 브랜치 connection string으로 실행해야 한다(사용자 확인 후) — 안 하면 승인 버튼/검색 진행 화면/후보 상세 모달이 500 에러를 낸다.
- **`GET /talent-search-policy/versions`의 50개 제한**: 프로젝트가 승인 시점에 고정한 `policyVersionId`가 최근 50개 버전 목록에서 밀려나면(기준 관리센터를 아주 많이 고치면) 그 프로젝트의 검토 화면·검색 진행 화면·대시보드 누적 추천 계산이 전부 "버전을 찾을 수 없음"으로 조용히 실패한다. 지금 사용 규모에선 가능성 낮지만, 실제로 발생하면 `GET /talent-search-policy/versions/:id` 전용 조회 엔드포인트를 추가하는 게 정공법이다(아직 안 만듦).
- **"오늘 추천"은 여전히 항상 0**: 검색 진행이 "한 번에 가상 후보 생성"하는 단발성 모델이라 하루 단위 배치 개념이 없어서다. 실제 배치 스케줄링 모델이 생기기 전까지는 억지로 숫자를 안 만든다.

## 4. 다음에 할 일 (원본 명세 로드맵 기준, 사용자 확인 필요)

원본 명세(`인재검색_자동화_마스터프롬프트_원본.md`) 907행 1E 항목 중 아직 안 만든 것: **일시정지·안전종료·이어서 검색, 체크포인트 저장과 복구, 검색배치 시뮬레이션(하루 단위 진행), 플랫폼 순서 제안, 검색범위와 인재풀 소진 보고서**. 이것들은 지금의 "한 번에 생성" 모델과 근본적으로 다른 데이터 모델(하루 단위 배치, 진행 상태 저장)이 필요해서 범위가 크다 — 착수 전에 사용자와 다시 스코프를 정해야 한다(예: 더 작은 하위 슬라이스로 쪼갤지, 아니면 1F(추천목록과 후보 상세)로 먼저 넘어갈지).

지난 세션 끝에 사용자에게 "여기서 정리할지 계속할지" 물었을 때 "며칠째 작업 중이라 일단 정리하고 다음 세션에서 계속하자"는 답을 받아서 이 문서를 남긴다 — **다음 방향은 아직 확정 안 됨, 새 세션에서 사용자에게 먼저 확인할 것.**

## 5. 사용자와의 협업 방식 (이번 세션에서 확인됨, 중요)

- **"그만 묻고 빨리 해줘" 류 피드백을 여러 번 받았다.** 매 슬라이스마다 진행 여부는 짧게 한 번만 확인하고(대개 "이대로 진행할까요?" 한 줄), 승인되면 설계→계획→구현→검증까지 중간에 재확인 없이 쭉 진행하는 게 맞다. 슬라이스가 끝난 뒤 다음 슬라이스로 넘어갈지만 다시 물어보면 된다.
- **AskUserQuestion 도구(선택지 버튼)가 이 세션에서 계속 응답을 못 받아왔다(user did not answer)** — 왜인지는 불명이지만, 반복해서 실패하면 그 도구를 포기하고 그냥 평문 텍스트로 짧게 질문한 뒤 사용자의 다음 메시지를 기다리는 쪽으로 전환할 것.
- 설명은 최대한 짧게, 개조식으로. 표/헤더보다 3~5줄 요약을 선호.
