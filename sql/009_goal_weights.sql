-- 2026-08-06: 기업/부서 목표에 "가중치" 개념을 도입한다. 사용자가 정의한
-- 성과관리 매커니즘: 같은 레벨(기업 목표끼리, 또는 한 부서의 부서 목표끼리)
-- 안에서 가중치 합이 100%가 되도록 배분하고, 기여율(가중치 × 달성률)의
-- 합으로 그 레벨의 "가중 성과율"을 낸다. 레벨끼리는 서로 가중치를 곱하지
-- 않는다 -- 기업 가중 성과율은 기업 목표끼리만, 부서 가중 성과율은 그
-- 부서의 부서 목표끼리만 계산한다.
--
-- NULL = 아직 가중치를 배분하지 않은 상태("가중치 설정 중"). 개인 목표·
-- 할 일에는 이번 단계에서 가중치를 적용하지 않는다(전체현황 화면부터
-- 단계적으로 적용하기로 함).
ALTER TABLE okrs ADD COLUMN IF NOT EXISTS weight integer;
